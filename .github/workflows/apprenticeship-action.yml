name: Apprenticeship-action

on:
    push:
        branches: [ "main" ]
    pull_requests: 
        branches: [ "main" ]
jobs:
    build:
      runs-on: ubuntu-latest
      steps:
      - name: run tests
        uses: liatrio/github-actions/apprentice-action@master